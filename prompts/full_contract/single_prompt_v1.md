# Full-Contract Single-Prompt v1

Read the epilepsy clinic letter and emit the final extraction schema.

Use only the letter text. Preserve uncertainty, cite exact evidence, and do not
invent missing clinical facts.

Return only valid JSON matching `schemas/final_extraction_v1.json`.
